---
deployment:
  tasks:
    - /bin/sh -c 'command -v composer > /home/corporacioncapsu/composer_check.txt 2>&1 || true'
    - /bin/sh -c 'composer --version >> /home/corporacioncapsu/composer_check.txt 2>&1 || true'